import { Component } from '@angular/core';

/**
 * Componente raíz de autenticación (AuthComponent).
 * Actualmente actúa como contenedor de rutas de login.
 * La lógica de recuperación de contraseña ha sido eliminada por no usarse.
 */
@Component({
  selector: 'app-auth',
  templateUrl: './auth.component.html',
  styleUrls: ['./auth.component.scss']
})
export class AuthComponent {}
