import { Component } from '@angular/core';

/**
 * Componente de layout para la zona de autenticación.
 * Sirve como contenedor para las rutas hijas como login o recuperación.
 */
@Component({
  selector: 'app-layout-page',
  templateUrl: './layout-page.component.html',
  styles: ['']
})
export class LayoutPageComponent { }
