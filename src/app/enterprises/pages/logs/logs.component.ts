import { Component } from '@angular/core';

/**
 * Componente de Logs.
 * De momento solo sirve como placeholder visual, pero en versiones futuras
 * se espera que muestre el histórico de accesos y acciones realizadas por los usuarios.
 */
@Component({
  selector: 'app-logs',
  templateUrl: './logs.component.html',
  styleUrls: ['./logs.component.css']
})
export class LogsComponent {
  // Sin lógica actualmente.
}
